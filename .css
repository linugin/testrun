<link rel="stylesheet" href="style.css">




/* ==========================
   Navigation Bar (No Dropdown)
========================== */
.main.nav {
  background-color: navy; /* Navy color */
  padding: 0;
}

.main.nav ul {
  list-style: none;
  margin: 0;
  padding: 0;
  display: flex;
  gap: 20px;
}

.main.nav ul li {
  position: relative;
}

.main.nav a.nav-card {
  display: block;
  padding: 12px 15px;
  color: white;
  text-decoration: none;
  font-weight: bold;
  border-radius: 4px; /* optional, gives links a “card” feel */
}

.main.nav a.nav-card:hover {
  background-color: #000080; /* Slightly darker navy on hover */
}
